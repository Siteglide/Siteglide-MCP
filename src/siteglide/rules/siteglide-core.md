# Prefer app/ as the project root (platformOS modern layout).
# siteglide-cli pull migrates marketplace_builder/ → app/ when needed (git mv in a git repo).
# Prefer Siteglide skills and docs for CMS/folder conventions when they conflict with plain platformOS docs.
# Use Siteglide-CLI auth (.siteglide-config) for ops tools — not Partner Portal / pos-cli credentials.
